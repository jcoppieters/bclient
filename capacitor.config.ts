import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.bouwsoft.blue',
  appName: 'bclient',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
